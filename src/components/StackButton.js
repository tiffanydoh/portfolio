import React from "react";

const StackButton = ({ text }) => {
  return (
    <button className="border-2 border-white text-[#F8F6F0] text-[15px] rounded-md p-1.5">
      {text}
    </button>
  );
};

export default StackButton;
